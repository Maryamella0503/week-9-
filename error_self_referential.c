#include <stdio.h>

 struct list {
    int data;
    struct list next_element; // we are creating an infiinte recursion
};

int main()
{
    struct list first_node;
    // Run this program. Learn from the error. 

    // Why the error. 
}